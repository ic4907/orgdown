export interface OrgdownConfig {
    appName: string,
    serverUrl: string,
    version: string
}

export default {
    "appName": "oRgdown",
    "serverUrl": "http://api.orgdown.com",
    "version": "1.0.0"
} as OrgdownConfig;